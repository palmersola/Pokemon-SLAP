<div class="center-wrapper">
    <h1 class="round-count">Level: <span id="level">{{player.dataValues.level}}</span></h1>
    <div class="game-container">
        <div class="game-over">
            <h1 id="gameOver">GAME OVER</h1>
        </div>

        <div class="player">
            <div class="player-sprite">
                <img src="{{player.dataValues.sprite}}" id="plySprt" alt="player's sprite">
            </div>
            <div class="player-stats">
                <span class="hp-stat" >{{playerName.dataValues.user_name}} HP: <span id="plyHp">{{player.dataValues.hp_stat}}</span></span>
            </div>
        </div>
        <div class="poke">
            <div class="poke-sprite">
                <img src="{{opponent.dataValues.sprite}}" id="oppSprt" alt="pokemon sprite">
            </div>
            <div class="poke-stats">
                <span class="hp-stat"> <span id="oppName">{{opponent.dataValues.pokemon_name}}</span> HP: <span id="oppHp">{{opponent.dataValues.hp_stat}}</span></span>
            </div>
        </div>
    </div>
    <h1>press spacebar to slap your opponent</h1>
    
    <div class="button-container">
        <button class="hidden replayBtn">play again</button>
    </div>
</div>
<script>
    let plyHp = {{player.dataValues.hp_stat}};
    let plyAtk = {{player.dataValues.attack_stat}};
    let plyDef = {{player.dataValues.defense_stat}};
    let plySpd = {{player.dataValues.speed_stat}}
    let plyLevel = {{player.dataValues.level}};
    let oppHp = {{opponent.dataValues.hp_stat}};
    let oppAtk = {{opponent.dataValues.attack_stat}};
    let oppDef = {{opponent.dataValues.defense_stat}};
    let oppSpd = {{opponent.dataValues.speed_stat}}


</script>
<script src="/js/front.js"></script>
   


